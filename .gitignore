.env

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# .NET build output
bin/
obj/

# Local API secrets (development database connection string, JWT signing key)
src/api/GraphPlatform.Api/appsettings.Development.json

# Test output
TestResults/

# macOS
.DS_Store

# Local object storage (FileSystemStorage default roots). Anchored: with core.ignorecase,
# a bare `data/` would also ignore the API's EF `Data/` folder.
/data/
src/api/GraphPlatform.Api/App_Data/
