# Fallback for statically-hosted production builds served WITHOUT the Docker
# entrypoint. Under Compose, /config.js (rendered from FRONTEND_* at container
# start) overrides this at runtime.
VITE_API_BASE_URL=/
